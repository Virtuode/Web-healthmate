// src/types/firebase.d.ts
declare module "firebase" {
  export const auth: any; // Replace 'any' with proper type if possible
  export const storage: any; // Replace 'any' with proper type if possible
  export const getDatabase: any; // Add this if needed
  export const ref: any; // Add this if needed
  export const onValue: any; // Add this if needed
}